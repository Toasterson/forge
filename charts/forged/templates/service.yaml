apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-forged
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app: forged
